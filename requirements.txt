# This project is validated on Python 3.11.x on Windows.
# Python 3.14 currently fails to build pygame because the Windows build expects distutils.
pygame==2.5.2
numpy>=1.24.0
matplotlib>=3.7.0

# agent/dqn.py uniquement (expérience DQN, non retenue -- voir NOTEBOOK.md essai 8).
# CPU suffit : pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0