__pycache__/
*.pyc
.venv/
venv/

# poids, logs et courbes générés localement -> pas de lourd dans le repo
runs/
*.npy
*.png
*.mp4

# exception : le meilleur agent + sa courbe, choisis à la main, sont petits (<70 Ko)
# et versionnés pour que le correcteur puisse les recharger sans relancer un entraînement
!deliverable/*.png

# éditeurs
.vscode/
.idea/
